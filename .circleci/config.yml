version: 2.1
setup: true
orbs:
  path-filtering: circleci/path-filtering@0.0.2
workflows:
  setup:
    jobs:
      - path-filtering/filter:
          base-revision: main
          mapping: |
            src/.*  first_condition true
            docs/.* first_condition false
          config-path: .circleci/workflows.yml
